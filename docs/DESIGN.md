# Website Design System (Canonical)

Canonical Bricks brand and design system docs live in `docs/_canonical/shared/brand/`:

- `brand-foundation.md`
- `brand-system.md`
- `design-system.md`
- `tokens.json`

This local file exists only as a stable pointer for work inside `Bricks-Web/`.

Website implementation notes:

- Use CSS custom properties in `styles.css` for tokens.
- Do not introduce new hardcoded hex values without updating the canonical tokens/docs first.
